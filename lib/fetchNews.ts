const fetchNews = async (
  category?: Category | string,
  keywords?: string,
  isDynamic?: boolean
) => {
  // GraphQL query
  // Fetch function chaching...
  // sort function by images cs not images present
  // return res
};

export default fetchNews;
